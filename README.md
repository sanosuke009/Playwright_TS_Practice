# Playwright_TS_Practice
A project to explore Playwright more with TypeScript

Run with the below command:

```sh
 npm run test:locators:report 
```
Install accessibility testing library command:
```sh
npm install @axe-core/playwright
```